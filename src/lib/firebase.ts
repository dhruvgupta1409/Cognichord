// Firebase integration removed for privacy and COPPA compliance.
// All user data is stored exclusively in browser localStorage and is never
// transmitted to any external server. See src/lib/db.ts for rationale.
export {};
